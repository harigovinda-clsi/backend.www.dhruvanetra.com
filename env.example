# DhruvaNetra Environment Configuration
# Copy this to .env and fill in your values
# Never commit .env to version control

# ===========================================
# DATABASE CONFIGURATION
# ===========================================

# MongoDB connection string
# Local development: mongodb://localhost:27017
# Docker compose: mongodb://mongodb:27017
# MongoDB Atlas: mongodb+srv://username:password@cluster.mongodb.net/
MONGODB_URL=mongodb://localhost:27017

# Database name
DATABASE_NAME=dhruva_netra

# ===========================================
# NASA API CONFIGURATION
# ===========================================

# Get your free API key from: https://api.nasa.gov/
# Note: DEMO_KEY has rate limits. Register for higher limits.
NASA_API_KEY=DEMO_KEY

# ===========================================
# SECURITY CONFIGURATION
# ===========================================

# JWT Secret Key - MUST be changed in production
# Generate secure key: python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# JWT Algorithm (HS256 recommended)
JWT_ALGORITHM=HS256

# Token expiration in minutes (1440 = 24 hours)
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# ===========================================
# APPLICATION CONFIGURATION
# ===========================================

# Server port
PORT=8000

# Log level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# ===========================================
# RENDER DEPLOYMENT (if using Render.com)
# ===========================================

# These will be automatically provided by Render
# Just ensure you set them in the Render dashboard

# For MongoDB Atlas on Render:
# MONGODB_URL=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>?retryWrites=true&w=majority

# For NASA API on Render:
# NASA_API_KEY=<your-nasa-api-key>

# For JWT Secret on Render:
# JWT_SECRET=<generate-secure-random-string>
